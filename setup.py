from setuptools import setup

__version__ = "0.3.5"

if __name__ == "__main__":
    setup(
        name="my-package",
        version=__version__,
        author="Phillip Todorov",
        # And so on...
    )
